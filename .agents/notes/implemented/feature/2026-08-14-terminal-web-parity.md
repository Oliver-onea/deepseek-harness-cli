# Agent Note: terminal parity with the web surface

Status: implemented

English | [中文](2026-08-14-terminal-web-parity.zh.md)

## Problem

`dsh` with no arguments opens the terminal, so the terminal is the surface a person meets first. Its interaction is far behind the web surface it shares an agent plane with, and the gap is not evenly distributed: some capabilities are absent, but a whole tier is already reachable and merely invisible.

Seven commands resolve in a shipped terminal session — `/compact`, `/exit`, `/feedback`, `/goal`, `/permission`, `/plan`, `/quit`. Nothing on screen reveals that any of them exist, what the current goal is, whether plan mode is on, or which permission preset applies. The web surface gives each of those a standing element.

Typing `/` or `@` does nothing. The web surface detects both under the caret and offers a grouped candidate menu ([`ui-input-trigger`](../../../../packages/client/ui-input-trigger/README.md)), which is how a reader discovers commands, skills, and file references without reading documentation. In the terminal, discovery has no entry point at all.

The terminal already depends on a framework that ships most of the missing mechanics. `dsh-tui` imports `Editor`, `ScrollView`, `Text`, `Loader`, `VStack`, `Markdown`, `TuiAltScreen`, `ProcessTerminal`, `matchesKey`, and `wrapTextWithAnsi` from `@earendil-works/pi-tui`. The `Editor` carries a complete autocomplete — provider-driven suggestions, debounced queries, keyboard arbitration, and an in-editor candidate list styled through the theme the shell already passes — so the input triggers need no second menu implementation; `SelectList`, `SettingsList`, and `Image` remain unimported and belong to Tier C surfaces.

## Decision

DeepSeek Harness ships terminal parity with the web surface in three independently shippable tiers. **Tiers A and B are implemented**; **Tier C is implemented for model selection** and otherwise deferred as recorded below.

### Tier C (model selection) — the terminal model picker

`/model` gives the terminal the same model-selection capability the web surface's [`ui-model-selection`](../../../../packages/client/ui-model-selection/README.md) two entries provide, in a terminal form: with no argument a keyboard panel over the conversation (numbered rows, current route marked, arrows or number to pick, `enter` to apply, `esc` to dismiss without cancelling a running turn or clearing queued prompts); with an argument a direct switch whose unknown ids fail loud naming the offered routes. Candidates come from the live `ctx.llm` adapter registry — the same source the web host serves as `session.models` — because the browser `ModelDirectoryResolver` is a client-side RPC cache the in-process terminal composition cannot and should not reach around; reading the registry directly keeps the two surfaces' catalogs identical. The catalog read also resolves each route's reasoning declaration (`resolveModelInfo` per model, the same per-model read the web host's `buildModelCatalog` performs), so effort candidates are the adapter's own, never a hardcoded list, and a provider whose exact-route resolution rejects lists as a failure row exactly as the web catalog's per-provider containment does. A picked route validates through `ctx.llm.resolveCallConfig` — mirroring the host's `session.selectModel` — and when queued or logged content carries an image, a model that states no image input is refused at pick time, mirroring the same handler's guard. The pick writes the `ModelSelectionRef` this front door installs, so the switch takes effect at the next prompt-assembly boundary and the next request's header records it durably; no new session event exists for the switch itself. A pick also persists as the deployment default through `ctx.agentDefaultModel.saveSelection()` — the web surface's own follow-the-picker rule — and a rejected save rides the switch notice as a warning rather than failing the pick.

Effort selection rides the same command, mirroring the web composer's two-level Model/Effort pick as a panel state plus an argument form: `→` on a route that declares efforts opens that route's effort tier (the model's default plus each declared effort, the effort that would apply marked; `esc` returns to the model list with its cursor intact — the tier is a state of the one panel, not a nested panel, so the shell's panel-count key guard covers both), and `/model <route> <effort>` pins an advertised effort directly while `/model <route> default` asks for the adapter's default; a route whose adapter declares no efforts offers no tier, no drill, and refuses any effort argument at pick time, and an unknown effort names what is available rather than failing at request time. A bare model pick follows the web composer's `selectionOf` rule — re-picking the current route keeps its effort, a route switch takes the new model's default — and the switch notice names a replaced effort (`was <effort>`) because the stored user section cannot distinguish an explicit effort from a materialized default. A `default` pick installs no effort, so each request resolves the adapter's live default and the persisted section's absent effort clears a stored one.

The selection itself resolves on every read through the same three layers the web surface's `selectionFor` follows: a pick made in this process, else the session's last logged request header, else the launch pin or the deployment default (read live). That read path is what makes `dsh --resume <session>` restore a route switched mid-session: the log's header outranks the creation options, and a blank session reads a default saved after it was created. The footer and pane title restate the route the moment the switch applies — and after replaying a resumed log's `request/context` — while the panel yields on a terminal too small for one candidate row, naming the direct-selection form.

### Tier A — surface what already resolves

The terminal footer reads standing session state from the services that already own it:

- [`@deepseek-ai/dsh-goal`](../../../../packages/goal/goal/README.md) supplies the current goal; completed goals are hidden, matching the web `GoalBar` behavior.
- [`@deepseek-ai/dsh-plan-mode`](../../../../packages/plan/plan-mode/README.md) supplies plan-mode state; the footer shows `plan` when the effective target is active and `plan*` while a pending `/plan` selection waits for the next accepted pre-step.
- [`@deepseek-ai/dsh-permission-presets`](../../../../packages/interaction/permission-presets/README.md) supplies the effective preset name, including the derived `custom` state.

[`dsh-tui`](../../../../packages/ui/tui/README.md) resolves each service through `ctx.get()` and passes a reader closure to `TerminalShell`. The readers are optional, so a composition without goal, plan-mode, or permission-presets simply omits the matching indicator. The footer continues to route every displayed string through `displayText()`, so untrusted model or tool text cannot repaint the screen through the new path.

### Tier B — the input trigger pipeline

The editor offers the two trigger menus on pi-tui's autocomplete ([`autocomplete.ts`](../../../../packages/ui/tui/src/autocomplete.ts)), matching the web surface's two typed sources:

- `/` at the start of the input lists the commands the **live** [`ctx.commands`](../../../../packages/interaction/commands/README.md) registry resolves for the driven agent, then the user-invocable skills the **live** [`ctx.skills`](../../../../packages/skill/skill/README.md) catalog serves it — the same roster the web `skill.list` RPC serves (the host filters `isUserInvocable` over the agent's scope and cwd, and a skill the model catalog omits is marked `user-only`, matching the web menu's marker). The provider reads both registries on every query, so a command or skill registered after the screen is up appears on the next keystroke and a shadowed or disposed one disappears, without a restart. Without the skill capability composed, `/` lists commands alone.
- `@` at a word boundary lists the **running subagent children** of this session — the same roster the web `@` source reads (`ctx.subagents.listChildren` filtered to running, matching [`ui-subagent`](../../../../packages/client/ui-subagent/README.md)). A child names itself by durable session title, else creation label, else id, mirroring the web session list's title-first ladder. Without the subagent capability composed, `@` offers nothing. File completion is deliberately absent: the web surface has no file-completion source, and a different feature under the same key would diverge the two surfaces.
- Candidate labels, descriptions, and completion values pass through `displayLine()` before the editor can draw or insert them, so registry text and model-influenced titles cannot carry control bytes to the screen or the draft.
- `/help` is a real command, registered beside `exit`/`quit`, whose text lists the live registry — the footer's `/help` hint names a command that resolves, and running it opens no model turn.
- A picked `/` candidate completes to the `/name ` line and submits in the same stroke; submission adjudicates exactly as the web composer does. A name the command registry resolves dispatches through it and never reaches the model; any other name — a skill — ships to the model verbatim as ordinary prompt text, and the pre-step boundary ([`dsh-tool-skill`](../../../../packages/skill/tool-skill/README.md)) recognizes the leading `/name` and injects the rendered body, so a menu pick, a hand-typed gesture, and a plain prompt are indistinguishable ([plain-text-reference decision](../architecture/2026-07-25-web-input-machine-and-slash-pipeline.md)). A picked `@` candidate inserts the reference `@name ` into the input, which ships verbatim the same way; agent-side consumption of `@` references is future business work on both surfaces.
- `esc` with a menu open dismisses the menu only: the shell routes the key to the editor before its interrupt branch, because `Agent.cancel` clears queued work, and a menu is not a reason to lose it. `ctrl+c` stays the single-key interrupt; the footer's running hint names `ctrl+c`, not `esc`.

Degradation: the menus clamp to the rows the footer, the editor, and one transcript row leave free (`menuRowsFor`), and a terminal with no room for a candidate row — the stress-tested 20x5 — never creates a menu, so there is no invisible list to render or capture keys; the trigger key stays inert and `enter` submits the literal text. `maxSuggestions` bounds the rows before that clamp; the palette renders both menus, so `color: false` draws the same layout unstyled.

Reachable subset: pi-tui's editor as it ships detects `/` only at the start of the first line and `@` only at a word boundary, renders one flat list, and applies an `enter`-confirmed completion in the same stroke as the submit. Inline `/` mid-line, `@` after punctuation, triggers on later lines, and grouped source headings would require forking the editor — rejected by the dependencies-over-hand-rolling policy and recorded as a stated divergence in `dsh-tui`'s README limitations.

### What parity does not mean

Terminal parity is capability parity, not layout parity. The web surface composes named slots (`conversation.input.dock`, `sidebar.workspaces`, `conversation.session.header.actions`); the terminal has one viewport, one composer, and a footer. A capability whose web form is a persistent card may take a footer field, a transient notice, or an overlay in the terminal, decided per capability against the reading it has to support.

## Alternatives considered

**Re-point the terminal at the JSON-RPC protocol first.** The terminal reads `ctx.agents`, `ctx.tools`, and `ctx.timer` directly, while [`dsh-sdk-jsonrpc-server`](../../../../packages/sdk/server/README.md) already serves the same agent plane to out-of-process clients. Making the terminal a protocol client is a real architectural improvement, and the protocol would need interactive methods it currently lacks — it carries `initialize`, `session/prompt`, and `shutdown` with `session.event` and `session.status` notifications, and no approval request, cancel, or steer method. That work is orthogonal to this one: how a capability is drawn does not depend on whether its data arrived through an injected service or a transport, so ordering parity first keeps the architectural option open without paying for it now.

**Point terminal users at the web surface.** This is the current de facto answer and it is why the terminal fell behind. It fails the case the terminal exists for: a person already in a shell, over SSH, or without a browser.

**Hand-roll the candidate menu.** Rejected on the same grounds as [the dependencies-over-hand-rolling policy](../../implemented/process/2026-07-26-dependencies-over-hand-rolling.md): pi-tui's autocomplete deletes owned code and tests, and a second suggestion implementation would drift from the editor's own key handling.

## Consequences

The `dsh-tui` package gained optional peer dependencies on `@deepseek-ai/dsh-goal`, `@deepseek-ai/dsh-plan-mode`, and `@deepseek-ai/dsh-permission-presets` so the footer can read authoritative state without re-folding session events. A terminal session now shows the current goal, plan mode, and permission preset when those services are composed, and each indicator disappears when its state is absent. The existing footer fields (run state, route, context occupancy, todo plan, queued depth, controls) keep their previous positions after the new indicators.

The terminal has one viewport, and every indicator competes with the transcript for rows. Tier A mitigates this by omitting completed goals, truncating long objectives, and hiding each indicator when its state is absent; Tier B's menus clamp to the rows the footer, the editor, and one transcript row leave free, and yield entirely on a terminal too small for one candidate row; the Tier C model picker reuses that row gate, and later Tier C surfaces will need their own degradation rules for the stress-tested 20x5 case.

The `/` menu lists commands then skills in one flat roster, grouped only by the registries' order, without the web surface's source-grouped headings.

## Verification

- `packages/ui/tui/tests/status.spec.ts` covers formatting for goal, plan mode, and permission preset, including omission when absent, and the footer's running hint naming `ctrl+c`.
- `packages/ui/tui/tests/shell.spec.ts` covers footer updates from the new readers, hides indicators when readers are absent, and drives the input-trigger menus end to end: live-roster offering and narrowing, skills listed beside commands with the `user-only` marker, a skill registered after wiring appearing on the next keystroke, a completed skill pick submitting as a prompt through no command dispatch, esc dismissing the menu without cancelling the agent (queued work survives), keyboard selection and tab completion, the `@` pick landing `@name ` verbatim, slash-pick submission through the registry, the row gate leaving the trigger inert on a too-small terminal while `enter` submits the literal text, row clamping, and identical menu layout with `color` off.
- `packages/ui/tui/tests/tui.spec.ts` mounts the plugin with real `dsh-goal` and `dsh-plan-mode` services plus a permission-preset reader and asserts the footer reflects live service state; it also proves `/help` answers from the live registry with no `user/message` or `turn/start` event, lists a command registered after mounting, lists a skill registered after mounting over the real `dsh-skill` registry and drops it when its registration disposes, marks a user-only skill while omitting a model-only one, escapes a control-byte skill description to visible `\xNN`, offers no skill rows when no skill capability is composed, submits a picked skill as a prompt with no `command/run` event, esc through the real input listener never reaches `Agent.cancel` while a menu is open (and still interrupts with none), a control-byte description reaching the screen only as visible `\xNN` escapes, the `@` menu listing running children through a composed roster and offering nothing without one, and that disposing the plugin fiber removes every terminal-owned command.
- `packages/ui/tui/tests/autocomplete.spec.ts` covers candidate derivation and sanitization for commands and skills (including an OSC-sequence description and the `user-only` marker), the name ladder and running classification for listed children, `@`-token extraction, the row budget, live-roster and live-catalog reads, catalog-failure containment, completion semantics for both triggers, and that file completion never triggers; `command-help.spec.ts` covers the `/help` listing text.
- `packages/ui/tui/tests/pty-boot.spec.ts` boots the shipped profile under a real PTY: the slash menu draws commands and the project's skills from the live composition, a picked skill submits as prompt text whose request carries the injected `<skill_content>` body beside the user's own words, `/help` prints the listing while the mock model serves zero requests and the footer stays at `0 tokens`, esc with a menu open keeps both a running turn and its queued follow-up reaching the model, and a 20x5 terminal shows no menu — no command and no skill row — while the input stays usable. The `/model` PTY cases drive the merged interaction harness (`apps/cli/tests/tui-interaction.harness.ts`): the picker lists the composition's advertised routes and esc over a running turn with a queued follow-up keeps both reaching the model, a numbered pick switches mid-session with the footer restating the route before any further request exists and the next request's wire body carrying the new model, the effort tier drills with `→`, lists the adapter's declared efforts with the default marked, returns to the list on `esc`, and pins an effort whose next conversation request carries it on the wire, esc from the tier keeps a running turn and its queued follow-up reaching the model, a direct `/model <id>` switches while an unknown id fails loud naming the offered routes, `/model <id> <effort>` pins and an unknown effort is refused at pick time naming what is available while the pinned selection survives a bare re-pick, a 20x5 terminal shows no picker while typed keys still reach the editor, and a two-boot `--resume` over one harness home restores a switched route from the persisted log with no pick involved.
- `packages/ui/tui/tests/model-picker.spec.ts` covers candidate derivation over a real `ctx.llm` registry (registration order, per-provider failure containment including an exact-route resolution failure, reasoning declarations with sanitized effort names, name sanitization), argument matching (unique id, `provider/model`, ambiguity, unknown), effort argument resolution (advertised id, the `default` sentinel, an advertised id literally named `default` outranking it, unknown ids naming what is available, routes offering none), selection application (`resolveCallConfig` validation, default-effort materialization, same-route effort and absence preservation, route-switch replacement, explicit pins, `default` installs, refusal keeping the previous route, the image guard over queued and logged content), default persistence (saved after install, rejected save degrading to a warning, absent persistence), the panel's rendering, scrolling, keyboard settlement, dismissal, the effort tier (drill, marking, `esc` returning to the list with the cursor intact, tier picks, no tier for effort-less routes, overflow), unnumbered overflow rows, and identical layout with `color` off in both states, and the command's degradation paths (no llm service, empty catalog, small-terminal row gate, usage errors).
- `packages/ui/tui/tests/tui.spec.ts`'s model-selection read-path suite covers the three-layer resolution on a real session (blank-session fallback through pin and default, the logged header outranking both, a pick outranking the log, the default read live), and mounts a resumed-shaped session whose replayed log names the footer, pane title, and the picker's marked route.

## Deferred

**Tier C — the remaining terminal screens.** Each item needs a surface the terminal does not have: [`ui-trajectory`](../../../../packages/client/ui-trajectory/README.md), [`ui-sidebar`](../../../../packages/client/ui-sidebar/README.md), [`ui-workspace`](../../../../packages/client/ui-workspace/README.md), [`ui-jobs`](../../../../packages/client/ui-jobs/README.md), [`ui-subagent`](../../../../packages/client/ui-subagent/README.md), [`ui-deliverables`](../../../../packages/client/ui-deliverables/README.md), [`ui-attachment`](../../../../packages/client/ui-attachment/README.md), and the `ui-settings` family. Two of these close limitations `dsh-tui` still records as deferred: the absent session switcher and inline images, the latter via pi-tui's `Image` component. Also deferred: the editor-level trigger detection itself (inline `/`, `@` after punctuation, later-line triggers, grouped headings), which needs an editor that supports them.

The launch-time resume picker — the discovery half of session switching, the follow-up the switcher rejection recommends — is implemented separately ([launch-time resume picker](2026-08-18-tui-launch-resume-picker.md)); it removes the need to know a session id at launch and does not close the in-terminal switcher item.
