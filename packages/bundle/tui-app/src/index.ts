/**
 * @deepseek-ai/dsh-tui-app — the terminal surface as a profile bundle. The
 * package's substance is `cordis.patch.yml` (declared by the `dsh.bundle.patch`
 * manifest field) plus the `./startup` command-line provider it composes; this
 * module carries no runtime API.
 * @module @deepseek-ai/dsh-tui-app
 */

export {}
